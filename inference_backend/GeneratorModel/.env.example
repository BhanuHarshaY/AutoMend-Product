# Copy this file to .env and fill in values before running docker compose

# GCP service account key JSON file path on the HOST machine
# This file is mounted read-only into the container
GOOGLE_APPLICATION_CREDENTIALS=/path/to/automend-trainer-key.json

# HuggingFace token -- only needed if pulling gated HF models
HF_TOKEN=
