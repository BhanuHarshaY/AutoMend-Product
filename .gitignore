node_modules/
.next/
.env
node_modules

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
backend/.env

# Throwaway helpers for manual testing walk-arounds (see DECISION-026).
# Kept at repo root deliberately — not part of the shipped chart / Dockerfiles.
rehash_spec.py
automend-rbac.yaml
slack-patch.yaml

# kind image-archive tarballs (intermediate output of `docker save` during manual testing)
*.tar

# Terraform (Phase 12) — state lives in GCS, never commit local copies.
# .terraform.lock.hcl IS checked in (pins provider hashes); explicitly kept out
# of this block.
**/.terraform/
**/terraform.tfvars
**/terraform.tfvars.json
**/*.tfstate
**/*.tfstate.*
**/*.tfplan
**/crash.log
**/crash.*.log

# Also gitignore the extra namespace RBAC helper from the demo push.
automend-extra-rbac.yaml

# Environment-specific helm value overlays (bucket name, SA email, etc.)
# not meant for source control.
model-storage-on.yaml

# Claude-related work notes + build history — local only, not part of the
# shipped codebase. Keeps the repo focused on runnable infra + app code.
.claude/
CLAUDE.md
PROGRESS.md
DECISIONS.md
build_plan.md
backend_architecture.md
MANUAL_TESTING.md
DEPLOY_GCP_QUICK.md
DEPLOY_GCP_TRUE.md
